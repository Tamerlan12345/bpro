PORT=3000
# Database Connection String
# Local Docker (default): postgres://postgres:password@db:5432/bpro
#
# Cloud Database (Neon / Supabase):
# IMPORTANT: For Neon/Supabase, ensure you use the Transaction Pooler URL if using serverless functions,
# or the Session Pooler/Direct URL for long-running servers.
# - Neon: postgres://user:password@ep-host.neon.tech:5432/dbname?sslmode=require
# - Supabase (Transaction): postgres://user.project_id:password@aws-0-region.pooler.supabase.com:6543/postgres?sslmode=require
#
# Note: "Tenant or user not found" error usually means the project ID (endpoint ID) is missing from the hostname or username.
DATABASE_URL=

SESSION_SECRET=supersecretkey
FRONTEND_URL=http://localhost:3000
OPENAI_API_KEY=
SPEECHMATICS_API_KEY=
GOOGLE_API_KEY=
NODE_ENV=development
